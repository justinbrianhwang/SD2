# Private project notes (not for publication)
SD2_project_overview.md

# Environments
.venv/
venv/

# External links (CARLA junction)
external/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.pytest_basetemp*/
build/
dist/

# Analysis outputs
outputs/*
!outputs/README.md

# Raw CARLA run recordings (regenerable; results are summarized in the README).
# Calibration fixtures under data/carla/calib/ stay tracked (not matched here).
# Both the top level and any subdirectory (e.g. data/carla/sd2_if/, sd2_sweep/).
data/carla/*.jsonl
data/carla/**/*.jsonl

# IDE / OS
.vscode/
.idea/
Thumbs.db
*.lnk

# Local E2E model repos + checkpoints (own licenses, large weights)
models/

# Recorder sidecars, run logs, and sweep outputs (data, not results)
data/carla/*.json
data/carla/**/*.json
data/carla/*.partial
data/carla/**/*.partial
data/carla/**/*.log
data/carla/sweep/
